var assert = require('assert');
describe('Create User', function() {
  describe('Create user succesly', function() {
    it('should return true when the user is create', function() {
      assert.equal([1,2,3].indexOf(4), -1);
    });
  });
});